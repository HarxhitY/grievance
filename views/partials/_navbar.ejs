<!-- <div class="container my-4">
    <nav class="navbar navbar-dark primary-color">
        <a class="navbar-brand" href="/">Grievance Portal</a>
            <% if(isAuthenticated) { %>
            <span id="logout-btn" class="navbar-text white-text my-0 mr-4">
                <a href="/users/logout">Logout</a>
            </span>
            <% } else { %>
                <span id="logout-btn" class="navbar-text white-text my-0 mr-4">
                <a href="/users/login">Login</a>
            </span>
            <% } %>
    </nav>

    <hr class="my-5">

</div> -->
<div class="container my-4">
    <nav class="navbar">
      <a class="navbar-brand" href="/">Grievance Portal</a>
      <% if (isAuthenticated) { %>
      <span id="logout-btn" class="navbar-text">
        <a href="/users/logout">Logout</a>
      </span>
      <% } else { %>
      <span id="logout-btn" class="navbar-text">
        <a href="/users/login">Login</a>
      </span>
      <% } %>
    </nav>
  
    <hr class="my-5">
  </div>

  
  <style>

.container {
  max-width: 1150px;
  margin: 0 auto;
}

.navbar {
  background-color: #333;
  color: #fff;
  padding: 1rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.navbar-brand {
  font-size: 1.5rem;
  color: #fff;
  text-decoration: none;
}

.navbar-text {
  color: #fff;
  margin-right: 1rem;
}

.navbar-text a {
  color: #fff;
  text-decoration: none;
}

hr {
  border-color: #ccc;
  margin-top: 0rem;
}

  </style>